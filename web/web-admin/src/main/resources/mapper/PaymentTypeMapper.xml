<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.admin.mapper.PaymentTypeMapper">

    <select id="customQueryList" resultType="com.atguigu.lease.model.entity.PaymentType">
        select * from payment_type where is_deleted = 0 and id in
        (select payment_type_id from room_payment_type where room_id = #{roomId} and is_deleted = 0)
    </select>
</mapper>
