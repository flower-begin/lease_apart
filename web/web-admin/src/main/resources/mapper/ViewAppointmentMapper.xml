<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.admin.mapper.ViewAppointmentMapper">

    <resultMap id="cMap" type="com.atguigu.lease.web.admin.vo.appointment.AppointmentVo">
        <id property="id" column="va_id"/>
        <result property="name" column="va_name"/>
        <result property="phone" column="va_phone"/>

        <association property="apartmentInfo" javaType="com.atguigu.lease.model.entity.ApartmentInfo">
            <id property="id" column="id"/>
        </association>
    </resultMap>

    <select id="customQueryList" resultMap="cMap">
        select ai.*, va.id va_id,
               va.user_id, va.name va_name,
               va.phone va_phone, va.appointment_time,
               va.additional_info, va.appointment_status
        from view_appointment va
                 left join apartment_info ai
                           on va.apartment_id = ai.id and ai.is_deleted = 0
        where va.is_deleted = 0
        <if test="queryVo.name != null">
            and va.name like concat('%', queryVo.name, '%')
        </if>

        <if test="queryVo.phone != null">
            and va.phone like concat('%', queryVo.phone, '%')
        </if>

        <if test="queryVo.provinceId != null">
            and ai.province_id = #{queryVo.provinceId}
        </if>

        <if test="queryVo.cityId != null">
            and ai.city_id = #{queryVo.cityId}
        </if>

        <if test="queryVo.districtId != null">
            and ai.district_id = #{queryVo.districtId}
        </if>

        <if test="queryVo.apartmentId != null">
            and ai.apartment_id = #{queryVo.apartmentId}
        </if>
    </select>
</mapper>
